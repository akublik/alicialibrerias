rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // WARNING: These rules are completely insecure and are for debugging only.
      // They allow anyone on the internet to read, write, and delete your files.
      // You MUST secure these rules before launching your app.
      allow read, write: if true;
    }
  }
}
