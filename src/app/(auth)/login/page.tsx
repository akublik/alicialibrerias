// src/app/(auth)/login/page.tsx
// Simplificado al máximo para depuración

export default function LoginPage() {
  return (
    <div style={{ padding: '20px', textAlign: 'center', border: '1px solid #ccc', borderRadius: '8px' }}>
      <h1>Login Page Test</h1>
      <p>Si ves esto, la página de login y el layout (auth) están funcionando a un nivel básico.</p>
    </div>
  );
}
