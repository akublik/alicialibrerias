// src/app/(auth)/login/page.tsx
// Intentionally simplified for debugging Internal Server Error

export default function LoginPage() {
  return (
    <div>
      <h1>Login Page Test</h1>
      <p>If you see this, the basic page rendering within AuthLayout is working.</p>
    </div>
  );
}
